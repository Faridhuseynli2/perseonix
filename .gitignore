# OS / junk
.DS_Store
**/.DS_Store
*.log

# Secrets — never commit these
.env
.env.*
!.env.example
test
# n8n playbooks have embedded API keys — never commit
n8n/

# Build / deps / data
node_modules/
.next/
.data/
perseonix-ui/.git/
